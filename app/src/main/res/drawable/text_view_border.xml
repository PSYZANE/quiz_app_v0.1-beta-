<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/white" /> <!-- Background color inside the border (white in this case) -->
    <stroke
        android:width="2dp"
        android:color="@color/light_blue" /> <!-- Border color and width (black, 2dp) -->
    <corners android:radius="14dp" /> <!-- Rounded corners (optional) -->
    <padding
        android:bottom="12dp"
        android:left="12dp"
        android:right="12dp"
        android:top="12dp" /> <!-- Padding inside the border (optional) -->
</shape>